{
  "name": "create-effect-http-starter",
  "version": "1.0.0",
  "description": "Scaffold a new Effect HTTP server project with schema-first API design",
  "type": "module",
  "bin": {
    "create-effect-http-starter": "dist/cli.js"
  },
  "files": [
    "dist",
    "templates"
  ],
  "scripts": {
    "build": "tsup src/cli.ts --format esm --out-dir dist --clean",
    "prepack": "bun run build"
  },
  "keywords": [
    "effect",
    "http",
    "server",
    "template",
    "starter",
    "scaffold",
    "create"
  ],
  "license": "MIT",
  "devDependencies": {
    "tsup": "^8.5.1",
    "typescript": "^6.0.3"
  }
}
